{
  "name": "tdd-sandbox",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "jest --watchAll --verbose --coverage",
    "e2e": "cypress open"
  },
  "devDependencies": {
    "vite": "^2.9.9"
  },
  "dependencies": {
    "@types/jest": "^28.1.1",
    "cypress": "^10.1.0",
    "jest": "^28.1.1"
  }
}
