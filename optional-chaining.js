const animalsA = {
    // dog: {
    //   name: `doggo`
    // }
    noise: (thing) => thing
    
  }
  
  console.log(animalsA.noise?.(`bark`))

  const animalsB = {
    // dog: {
    //   name: `doggo`
    // }
    noise: (thing) => thing
    
  }
  
  console.log(animalsB.noise?.(`bark`))