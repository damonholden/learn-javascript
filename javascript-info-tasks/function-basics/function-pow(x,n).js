function pow(x,n) {
  return x ** n;
}

let number = prompt(`what number do you want to be powered?`, ``);
let power = prompt('what do you want the number to be powered by?', ``);

alert(pow(number, power));
